const SupportAction = ({description}) => {
  return (
    <div className="text-sm">
      <a href="#" className="support-link">
        {description}
      </a>
    </div>
  );
};

export default SupportAction;